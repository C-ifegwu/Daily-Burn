// Legacy set_budget_screen replaced by new input_budget_screen → select_date_screen flow.
import 'package:flutter/material.dart';
import '../../theme/app_theme.dart';

class SetBudgetScreen extends StatelessWidget {
  const SetBudgetScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(backgroundColor: AppTheme.background, body: const Center(child: Text('Redirecting...')));
  }
}
